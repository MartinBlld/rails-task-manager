<%= form_with model: @task do |t| %>
  <%= t.label :Titre %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <%= t.label :Fait? %>
  <%= t.check_box :completed %>
  <%= t.submit :soumettre %>
<% end %>

